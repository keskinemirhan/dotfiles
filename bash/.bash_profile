#
# ~/.bash_profile
#

[[ -f ~/.bashrc ]] && . ~/.bashrc

#if [ -z "$DISPLAY" ] && [ "$XDG_VTNR" = 1 ]; then
#    exec startx
#fi

# [[ -z $HYPRLAND_INSTANCE_SIGNATURE ]] && Hyprland &> ~/hyprland_log


